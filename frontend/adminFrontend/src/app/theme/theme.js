import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    allVariants: {
      fontFamily: '"Inter", sans-serif',
      textTransform: "none",
      fontSize: 16,
    },
  },

});